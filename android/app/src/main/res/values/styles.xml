<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash / cold‑start theme (must inherit from AppCompat) -->
    <style name="LaunchTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Shows your launch background until Flutter draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <!-- Recommended Stripe flags -->
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <!-- Theme used after Flutter is up (must inherit from MaterialComponents) -->
    <style name="NormalTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
